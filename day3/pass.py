#!/usr/bin/env python

# pass statement

# x = 18
# if x >= 18:

# suppose after the line #6, in next line(#7) we don't want to write anything, which we don't do in real-time, but suppose,
# Use:
x = 18
if x == 18:
    pass

# If we don't write anything after line #6, it gives error while running.
# But if we write "pass" as line #12, it doesn't give any error. It doesn't give any output also.
